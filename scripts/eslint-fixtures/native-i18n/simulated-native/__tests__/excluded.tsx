import { View } from 'react-native'

/** Intentionally hardcoded — excluded via __tests__ ignore in regression tests. */
export function NativeI18nExcludedTestsFixture() {
  return <View accessibilityLabel="Test harness label" headerTitle="Books" />
}
