import { View } from 'react-native'

/** Intentionally hardcoded — excluded via *.test.tsx ignore in regression tests. */
export function NativeI18nExcludedTestFileFixture() {
  return <View placeholder="Search" title="Fixture" />
}
