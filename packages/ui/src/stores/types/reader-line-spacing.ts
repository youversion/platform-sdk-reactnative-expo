// Mirrors the unexported `BIBLE_READER_SPACING` in `@youversion/platform-react-ui`.
// Values must match exactly so the Web SDK accepts our `lineSpacing` and cycles correctly.
export const READER_LINE_SPACING = {
  SM: 1.45,
  DEFAULT: 1.7,
  LG: 2.0,
} as const
