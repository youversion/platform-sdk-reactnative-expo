---
'@youversion/platform-react-native-expo-ui': patch
---

fix: omit verse-action highlight colors when `auth` is not configured, so a kids app without sign-in still gets Copy and Share instead of dead swatches.
