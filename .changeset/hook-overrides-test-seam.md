---
"@youversion/platform-react-native-expo-core": minor
"@youversion/platform-react-native-expo-ui": minor
---

Export `hookOverrides` on `YouVersionProvider` as a test seam, plus the `HookOverrides` and `AuthContextValue` types. Production apps leave `hookOverrides` unset.
